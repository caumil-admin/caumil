# PLAN.md — 진행 상황과 다음 일

기준일 2026-09-23. 절차는 `docs/WORKFLOW.md`, 규칙은 `CLAUDE.md`, 화면은 `DESIGN.md`.

## 목표
공유 드라이브 제출 폴더 4곳의 개인·팀 논문을 경진대회 엑셀의 6개 변수로 채점하고, 가중치 노트북의 사전함수로 순위를 매긴 사이트를 운영한다. 새 원고가 오면 같은 절차로 갱신한다.

## 끝난 것
- [x] 원고 15편(개인 12·팀 3) 추출·채점·순위. 기본 가중치에서 개인 VT > HS > TE, 사람별 최석철 > 이건희 > 서보윤, 팀 DR > MP > ON. 프리셋 4종 모두에서 순위 안정.
- [x] 파이프라인을 이 머신에 재구성(표준 라이브러리만, olefile 벤더링). `score.py` 가 원본 결과를 결정적 부분은 정확히, 몬테카를로는 0.013 이내로 재현.
- [x] 공개 저장소용 데이터 정리: 드라이브 ID 는 해시(`driveKey`)로만, 공개판에는 인용문·원문 링크 없음, `noindex`.
- [x] Pages 배포 체계: `./run.sh deploy` → 커밋 → caumil-admin 으로 푸시. 스크립트 주소에 내용 해시.
- [x] v3 사이트: Claude Design 캔버스의 6개 아트보드 구현(순위 탐색·범주 개요·지표 산출·원고 프로필·모바일), 보라색 톤, 표 머리글 보라·흰 글씨, 상위 2편 강조, 1위·2위 안·3위 안 확률, 라이트·다크.
- [x] v2(캔버스 적용 전 페이지) 보관본을 `v2/` 로 함께 배포, v1 은 `v1/` 유지. 헤더·푸터 버전 전환.
- [x] 내부용 아티팩트 재게시(원문 링크·투고 전 확인 패널 포함).

## 다음 일
1. **투고 전 조치 전달** — 심각도 높은 리스크 10건(이시연 NM–NH 중복게재 위험, 최석철 VT 블라인드 위반 소지, 서보윤 TE 서지 오류, 강줄기 AD 참고문헌 오류, 6조 MP 템플릿 소속 오기재 등)은 각 원고 프로필의 내부용 패널과 `eval/<ID>.json` 의 `flags` 에 있다. 저자에게 전달할지, 어떤 형식으로 전달할지는 사용자 결정.
2. **새 원고·수정본 처리** — `docs/WORKFLOW.md` 1~6단계. 드라이브 목록은 세션의 Drive 커넥터로 만든다. 수정된 원고는 `check.py` 가 "텍스트가 평가 이후 바뀜"으로 잡는다.
3. **선택** — 원고 점수를 엑셀과 같은 형식의 시트로 내보내 노트북 재학습에 쓰기(노트북의 '현재안 1개' 제약을 먼저 풀어야 함). hwp 를 PDF 로 바꿔 그림까지 확인하기.
4. **선택** — 아티팩트 게시 시 CSV 내려받기 경고: `downloads` 기능을 선언하거나 아티팩트 빌드에서 내려받기 코드를 빼면 경고가 사라진다(현재는 아티팩트에서 '복사'로 동작).
5. **선택** — 캔버스 제목 "CAUMIL 논문 순위 v2" 를 v3 로 바꾸거나, 캔버스에 보라색 토큰·표 머리글·상위 2편 변경을 반영해 디자인 원본과 구현을 맞추기.

## 결정 사항(바꾸려면 사용자 확인)
- 순위는 닫힌식 사전함수. 기본 가중치는 합 1 정규화, 원본(합 0.92)은 프리셋. 상호작용 항은 노트북 주석 의도대로.
- 계획서 점수와 원고 점수는 한 표에 섞지 않는다. 리스크는 점수 밖.
- 평가 원자료 공개. 원고 원본·전문 텍스트·드라이브 목록·ID 비공개.
- 루트는 공개판 v3, `v2/`·`v1/` 보관. 내부용은 아티팩트.

## 알려진 제약
- 이 WSL 머신에는 pip·make·Node(WSL)·Chrome 확장이 없다. Windows 쪽 Chrome·Node 는 `/mnt/c/...` 로 쓸 수 있다.
- GitHub 푸시는 URL 에 `caumil-admin@` 을 붙여야 한다(개인 계정 자격 증명이 먼저 잡힘).
- 채점은 텍스트 근거만이므로 그림에만 있는 결과는 반영되지 않는다.
