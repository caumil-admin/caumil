# DESIGN.md — CAUMIL 논문 지표 v3 화면 규격

원본은 Claude Design 캔버스 "CAUMIL 논문 순위 v2"(`https://claude.ai/artifact/9tBbBaMpePcUWcuFiKSsCm`)의 아트보드 6장(순위 탐색·범주 개요·지표 산출·원고 프로필·모바일 순위·모바일 프로필)이다. 구현은 `site/template.html`(토큰·CSS·뼈대)과 `site/app.js`(렌더링)이며, 캔버스와 다른 점은 맨 아래에 적었다.

## 원칙
- 학술 지표 색인(인용 지표 사이트)처럼 읽힌다: 표가 중심, 숫자는 고정폭, 장식 최소.
- 적합도는 "노트북의 경험 기반 점수이며 실제 우승 확률이 아니다"를 화면마다 한 번은 말한다.
- 상위 2편(1·2위)을 범주마다 강조한다. 1등만 뽑는 화면이 아니다.
- 공개판과 내부용은 같은 화면이되 내부용에만 원문 링크와 '투고 전 확인' 패널이 있다.

## 토큰 (`:root`)
| 역할 | 라이트 | 다크 |
|---|---|---|
| 바탕 `--bg` | #F6F5FA | #12101C |
| 카드 `--surface` / 표 머리 보조 `--surface-2` / 줄무늬 `--zebra` | #FFFFFF / #EFEDF5 / #FAF9FC | #1A1727 / #211D33 / #1D1A2C |
| 글자 `--ink` / 보조 `--muted` | #1B1A2E / #5B5872 | #ECEAF5 / #A39FBA |
| 선 `--rule` / 진한 선 `--rule-2` | #DFDCE9 / #C7C3D6 | #2F2A45 / #3D3757 |
| 강조 `--accent` / 호버 / 연한 바탕 `--accent-soft` | #56489E / #3E3378 / #ECE9F7 | #A99AE6 / #C4B9F0 / #2A2447 |
| 히트맵 `--accent-rgb` | 86, 72, 158 | 169, 154, 230 |
| 표 머리글 `--thead-bg` / 글자 `--thead-fg` | #56489E / #FFFFFF | #3B3170 / #ECEAF5 |
| 상승 `--up` / 하락 `--down` | #0E6B5C / #9E3B2A | #5FCFB6 / #F09A88 |
| 사분위 Q1·Q2·Q3·Q4 (글자/바탕) | #0E6B5C/#E0F1EC · #356B25/#E8F2DF · #845A08/#F6EDD6 · #9E3B2A/#F6E3DE | #7FD8C4/#12332C · #A9D68E/#1E2E15 · #E8C57A/#3A2C10 · #F0A090/#3B1F1A |
| 수정 배지 `--upd-fg/bg`, 선택 칩 `--chip-ink/--on-chip` | #845A08/#F6EDD6, #1B1A2E/#FFFFFF | #E8C57A/#3A2C10, #ECEAF5/#12101C |

다크는 `@media (prefers-color-scheme: dark)` 의 `:root:not([data-theme="light"])` 와 `:root[data-theme="dark"]` 두 곳에 같은 값을 둔다. 색을 추가하면 세 블록 모두에 넣는다.
사분위 색은 의미색이라 톤 변경에서 제외한다. 히트맵 셀 배경은 `rgba(var(--accent-rgb), 0.05 + 0.30 × 값)`.

## 서체
- 본문 IBM Plex Sans KR(400/500/600/700), 원고 제목 Noto Serif KR(600/700), 숫자·ID IBM Plex Mono(500/600). Google Fonts 한 줄로 불러오고 시스템 대체 스택을 둔다.
- 기본 14px/1.55. 페이지 제목 28px(모바일 22px), 프로필 제목 30px 세리프(모바일 21px), 카드 제목 15px, 보조 12~12.5px, 표 머리글 11.5~12px.
- 숫자 열은 `tabular-nums`.

## 레이아웃
- 최대 폭 1440px, 좌우 여백 32px(700px 이하 16px). 헤더 60px 고정(sticky), 흰 바탕, 아래 1px 선.
- 헤더: 브랜드(CAUMIL 모노 + 논문 지표) · 메뉴(순위 탐색·범주 개요·지표 산출, 현재 항목은 강조색 밑줄) · 검색 300px · 버전 전환(v3 · YYYY.MM 선택 = 짙은 칩, v2·v1 보관 링크).
- 페이지 머리: 흰 띠, 이동 경로 12px, 제목, 설명 13px, 오른쪽에 기준 시점·원고 수·신규/수정·가중치 dl.
- 순위 탐색: 264px 필터 열(범주 버튼, 학회 체크, 가중치 라디오, 사분위 범례) + 표. 표 열: 52 / 원고(1fr) / 84 / 64 / 64 / 64 / 46×6 / 104px, 행 최소 72px, 줄무늬. 1·2위 행은 왼쪽 3px 강조 띠와 강조색 순위 숫자.
- 범주 개요: 열 1fr / 56 / 84 / 84 / 272(분포) / 176(상위 2편) / 46×6 / 80px. 분포 띠는 점 하나가 원고 한 편, 검은 눈금이 중앙값, 1위 ID 라벨. 하위 학회 행은 들여쓰기·줄무늬.
- 지표 산출: 왼쪽 1fr(계산식·변수와 가중치·지표 정의·원천 자료) + 오른쪽 600px(가중치 실험·적합도 곡선). 1200px 이하 한 열.
- 원고 프로필: 머리(경로·이전/다음·칩·세리프 제목·영문 제목·저자·업로드/평가) → 지표 타일 6개 → 본문 1fr(범주별 순위·지표 구성·평가 요약·보완/대표 결과) + 400px 옆 열(순위 안정성·다음 0.05·계획서 대비·[내부용: 투고 전 확인]·같은 범주 원고).
- 표 머리글은 보라 바탕(`--thead-bg`)에 흰 글씨. 정렬 가능한 열은 버튼이며 활성 열은 굵게+밑줄.

## 반응형
- ≤1200px: 지표 산출·프로필 한 열, 검색 220px.
- ≤980px: 순위 필터가 표 위로 올라가 가로 배열, 지표 타일 3열, 2단 카드 1열.
- ≤700px: 메뉴가 둘째 줄로, 검색은 페이지 머리 안, 학회·가중치 필터 숨김, 범주는 3칸 분할 버튼. 순위표는 카드(순위 / 제목·저자·학회·배지 / 적합도+사분위 칩)로 바뀌고 1·2위 카드는 강조색 테두리. 지표 구성은 `<details>` 로 접힘. 범주 개요 표는 가로 스크롤.
- 페이지 본문은 가로 스크롤이 생기지 않아야 한다(표만 자기 컨테이너에서 스크롤).

## 지표 표기 규칙
- 적합도 % 는 상호작용 항이 있는 가중치에서만 1차 지표. 없는 가중치(균등·HW 제외)는 환산 점수가 1차, 적합도가 2차.
- 백분위 = (N − R + 0.5) ÷ N × 100, 사분위 = 75/50/25 초과 기준. 학회를 거르면 남은 원고로 다시 매기고 순위 범위는 숨긴다.
- 순위 범위는 P10–P90, 확률은 1위·2위 안·3위 안. 프리셋 4종은 4,000회 사전 계산값, 사용자 가중치는 브라우저 1,500회.
- 계획서와 다른 주제(`projectMatch: different`)는 계획서 점수를 보여 주지 않는다.

## 접근성·상호작용
- 실제 `<button>`·`<a>`·`<input>`+`<label>`. 정렬 버튼에 `aria-label`, 눌린 상태는 `aria-pressed`, 현재 메뉴는 `aria-current`. 포커스 링은 강조색(보라 머리글 위에서는 흰색).
- 해시 라우팅: `#rank`, `#rank-<personal|people|team>[-<ee|it|dt>]`, `#categories`, `#method`, `#<원고 ID>`. 범주 선택은 `localStorage` 에 기억(실패해도 동작).
- CSV: Pages 에서는 내려받기, 아티팩트에서는 클립보드 복사(샌드박스가 내려받기를 막음).

## 캔버스와 다른 점
1. 색: 파랑(#1D5B94)에서 보라(#56489E)로, 중립색도 보라 기운으로. 다크 테마 추가.
2. 표 머리글: 회색 바탕 → 보라 바탕·흰 글씨.
3. 상위 2편: 범주 개요 '1위 원고' → '상위 2편'(1·2위), 순위표 1·2위 띠, 프로필 '1위 확률 · 2위 안', 안정성에 '2위 안 확률'.
4. 버전 전환이 v2/v1 두 개에서 v3/v2/v1 세 개로.
5. 내부용 빌드에만 '투고 전 확인' 패널(리스크·자동 점검·원문 링크). 공개판·캔버스에는 없음.
6. 아티팩트에서는 CSV 버튼이 '복사'.
7. 지표 구성 표는 데스크톱에서 5열 표, 모바일에서 접히는 목록 두 가지 DOM 으로 그린다(캔버스는 각각 한 가지).
